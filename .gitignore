node_modules/
preprocess/.venv/
.env
local.env
.DS_Store
*.zip
.idea/
eval/images/
__pycache__/
usage.json

# Runtime artefacts — the LaunchAgent appends to this on every restart.
*.log

# Terraform: local state and tfvars hold infrastructure detail, not secrets,
# but neither belongs in the repo.
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/terraform.tfvars
deploy/terraform/.terraform.lock.hcl
